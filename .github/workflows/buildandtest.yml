name: CI

on: [push]

jobs:
    build:
        runs-on: macOS-latest
        steps:
          - name: Checkout 
            uses: actions/checkout@v1
          - name: Build and Test
            run: |
                cd "Framework"
                echo $(pwd)
                gem install xcpretty
